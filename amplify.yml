version: 1
applications:
  - appRoot: portfolio
    frontend:
      phases:
        preBuild: [npm ci]
        build: [npm run build]
      artifacts:
        baseDirectory: dist
        files: ['**/*']
      cache:
        paths: [node_modules/**/*]
